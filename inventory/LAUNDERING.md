# Blanchiment — items inventaire

Placeholders pour le flow 3 étapes (`pn-corecreator` laundering) :

| Fichier | Item |
| --- | --- |
| `dirty_money_pack.png` | Paquet de billets sales |
| `washed_bills.png` | Billets lavés |

Remplacer par des assets définitifs puis commit/push dans `lumarp.images` (CDN jsDelivr).
